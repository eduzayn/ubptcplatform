import React, { useState } from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import {
  Search,
  Filter,
  Plus,
  Edit,
  Trash2,
  Eye,
  MoreVertical,
  Upload,
  Menu,    // Adicionado
  Shield,  // Adicionado
  Bell     // Adicionado
} from "lucide-react";
import {
  Avatar,
  AvatarImage,
  AvatarFallback
} from "@/components/ui/avatar";  // Nova importação
import { Link } from "react-router-dom";  // Nova importação
// ... resto das importações existentes ...
